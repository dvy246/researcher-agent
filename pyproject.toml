[project]
name = "researcher-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.3.9",
    "ipykernel>=7.1.0",
    "langchain>=1.2.8",
]
